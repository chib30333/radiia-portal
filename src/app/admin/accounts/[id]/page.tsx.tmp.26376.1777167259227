import { AccountDetailView } from "@/features/admin/views/AccountDetailView";

export default function AdminAccountDetailPage({ params }: { params: { id: string } }) {
  return <AccountDetailView accountId={params.id} />;
}
